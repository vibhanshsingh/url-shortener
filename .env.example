# Copy this to .env before running `docker compose up`.
# .env itself is gitignored — never commit real credentials, even for local dev.
# This is the pattern we'll extend with a real secrets manager in Milestone 15.

POSTGRES_USER=shortener_user
POSTGRES_PASSWORD=change_me_locally
POSTGRES_DB=url_shortener
